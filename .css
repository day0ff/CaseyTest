.highlight {
  color: green;
}

.diagramm, .pie{
  width: 100px;
  float:left;
  text-align: center;
  margin: 20px;
}
.diagramm span{
  font-weight: bolder;
  font-size: 1.2em;
}
.pie {
  vertical-align: middle;
  position: relative;
}

.pie span {
  position: relative;
  display: block;
  top: 42px;
  z-index: 1;
}

svg {
  border-radius: 50%;
  display: block;
  position: relative;
  top: -20px;
  z-index: 0;
  transform: rotate(-90deg);
}

circle {
  stroke-width: 16;
}
