.highlight {
  color: green;
}

.diagramm, .pie{
  width: 100px;
  /* border: solid 1px green; */
  text-align: center;
}
.diagramm span{
  font-weight: bolder;
  font-size: 1.2em;
}
.pie {
  vertical-align: middle;
  position: relative;
}

.pie span {
  position: relative;
  display: block;
  top: 42px;
  z-index: 1;
}

svg {
  border-radius: 50%;
  display: block;
  position: relative;
  top: -20px;
  z-index: 0;
  transform: rotate(-90deg);
}

circle {
  stroke-width: 16;
}
